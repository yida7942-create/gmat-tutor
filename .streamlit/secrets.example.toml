# 这是 secrets 模板，实际 secrets 在 Streamlit Cloud 后台配置
# 本地使用时复制到 .streamlit/secrets.toml

[ai]
api_key = "your-api-key-here"
base_url = "https://ark.cn-beijing.volces.com/api/v3"
model = "doubao-seed-1-6-251015"
